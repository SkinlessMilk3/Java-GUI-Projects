package EventListeners;

import Events.*;

public class KeyPressedListener implements KeyListener{

    public void onKeyPressed(KeyEvent event) {
        handle(event);//seems right not entirely sure though
    }

    public void handle(KeyEvent event){ }
}
